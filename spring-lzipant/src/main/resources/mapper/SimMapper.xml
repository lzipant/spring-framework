<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lzip.ant.mapper.SimMapper">

	<select id="listByEcId" resultType="net.lzip.ant.model.Sim" parameterType="int">
		SELECT * FROM sim WHERE ec_id = #{ecId}
	</select>


</mapper>
